[project]
name = "onotlogy-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb,hub]>=1.27.2",
    "ibis-framework[duckdb]>=12.0.0",
]
